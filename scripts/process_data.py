"""
Placeholder script to process raw NZ Police data into the JSON format
expected by the React frontend in public/data/.

In production this would parse CSV exports from policedata.nz,
normalize the data, and write out the JSON files.
"""
import os

print("process_data.py: No processing configured — using existing public/data/ files.")
print("To implement: parse CSV files from download_data.py and write to public/data/")
